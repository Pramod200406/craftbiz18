{
  "name": "CRAFTBIZ AI (SIH 2026)",
  "image": "mcr.microsoft.com/devcontainers/universal:2",
  "forwardPorts": [
    5173,
    8000
  ],
  "portsAttributes": {
    "5173": {
      "label": "CRAFTBIZ Frontend",
      "onAutoForward": "openPreview"
    },
    "8000": {
      "label": "FastAPI Backend",
      "onAutoForward": "silent"
    }
  },
  "postCreateCommand": "pip install -r backend/requirements.txt && cd frontend && npm install",
  "postStartCommand": "python backend/seed.py && (cd backend && python -m uvicorn main:app --host 0.0.0.0 --port 8000 &) && (cd frontend && npm run dev -- --host &)",
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-python.python",
        "dbaeumer.vscode-eslint",
        "bradlc.vscode-tailwindcss"
      ]
    }
  }
}